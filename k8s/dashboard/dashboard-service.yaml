apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: coldchain
  labels:
    app: dashboard
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3000
      name: http
  selector:
    app: dashboard